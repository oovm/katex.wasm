{
    "private": true,
    "scripts": {
        "build": "node esbuild",
        "test": "wee build && wasm-pack test --node"
    },
    "dependencies": {
        "katex": "^0.12.0"
    },
    "devDependencies": {
        "@types/katex": "^0.11.0",
        "esbuild": "^0.7.17"
    }
}
